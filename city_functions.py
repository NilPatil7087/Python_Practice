def city(city_name,country_name):
    info =f"{city_name},{country_name}"
    return info.title()

def city2(city_name,country_name,population):
    info2 = f"{city_name},{country_name},population:{population}"
    return  info2.title()
